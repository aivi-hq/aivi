---
'@aivi/cli': minor
---

`aivi link [PLATFORM]` mints a one-time link code over HTTP and prints exactly where to spend it — the command runs on the person's machine and needs only the client config, no installed server. With several channel modules running it asks which hint to show; an unknown platform is refused with the list of running ones.
