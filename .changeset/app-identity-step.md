---
'@aivi/app': patch
---

`server create` becomes the identity step behind `aivi setup` and its output feeds the flow instead of addressing the person: the `next` instructions name commands that exist (`aivi setup`, `aivi serve`). `aivi people create` on a terminal offers to mint the person's token right away — the common reason to create a person.
