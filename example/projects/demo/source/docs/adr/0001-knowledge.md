# Project knowledge belongs with the project

This example project keeps decisions in `docs/adr`, which aivi's convention
indexes as `decision`; the rest of `docs/` is `doc`. The repository carries
nothing of aivi's: the home describes the project. These documents are
authoritative; search indexes are rebuildable derivatives.
