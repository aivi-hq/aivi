# Project knowledge belongs with the project

This example project keeps decisions in `docs/adr`. Research and other source
paths can be added to `aivi.project.json`. These documents are authoritative;
future search indexes will be rebuildable derivatives.
