node_modules/
*.tsbuildinfo
.aivi/
*.sqlite*
.env
.env.*
!.env.example
*.tgz
.DS_Store
# The live aivi config is edited by aivi and its operator; only templates are tracked.
aivi.json

# The live home's projects are its own checkouts: never repo content, never linted.
example/projects/
example/state/

# Build output: compiled packages (every exports map points at dist/) and
# their build info.
dist/
